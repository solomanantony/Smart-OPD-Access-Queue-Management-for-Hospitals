# Hospital Token System - Hackathon MVP

## Run (local)
1. Create MySQL DB using the SQL script (`database/schema.sql`) or run the provided SQL.
2. Update `config.py` with your DB credentials.
3. Install Python dependencies:
